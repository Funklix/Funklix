'use strict';
const route = require('../../social-publishing-route');
module.exports = async function handler(req, res) { const serverRequestId = route.requestId(req); let jobId=''; if (req.method !== 'POST') return route.send(res, 405, { ok: false, status: 'request_invalid', classification:'method_not_allowed', serverRequestId }); const identity = route.actor(req); if (!identity) return route.send(res, 401, { ok: false, status: 'authentication_required', serverRequestId }); try { const body = await route.readBody(req); if (!route.validInput(body, true)) return route.send(res, 400, { ok: false, status: body.confirmed === true ? 'request_invalid' : 'confirmation_required', serverRequestId }); const result = await route.service().publish({ ...body, ownerAccountId: identity.ownerAccountId, serverRequestId, responseLanguage: req.headers?.['accept-language']?.startsWith('de') ? 'de' : 'en' }, identity.user); jobId=result.jobId||''; const pending=['publishing_in_progress','reconciliation_required','provider_accepted_unreconciled','outcome_unknown'].includes(result.status); try{return route.send(res,result.ok?200:pending?202:422,{...result,serverRequestId,clientRequestId:body.clientRequestId,retryable:jobId?false:result.status==='failed',diagnostic:result.diagnostic||route.safeDiagnostic({clientRequestId:body.clientRequestId,serverRequestId,publishJobId:jobId,providerAttemptId:result.providerAttemptId,phase:result.ok?'external_post_committed':'publish_stopped',classification:result.status,retryable:false,committedStateCategory:result.ok?'external_post_committed':jobId?'job_committed':'none'})});}catch{return route.authoritative.emergency(res,{server_request_id:serverRequestId,job_id:jobId});} } catch { if(jobId)return route.authoritative.emergency(res,{server_request_id:serverRequestId,job_id:jobId}); return route.send(res,503,{ok:false,status:'storage_unavailable',classification:'request_processing_failed',serverRequestId,retryable:true}); } };
