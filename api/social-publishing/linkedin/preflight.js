'use strict';
const route = require('../../social-publishing-route');
module.exports = async function handler(req, res) { const serverRequestId = route.requestId(req); if (req.method !== 'POST') return route.send(res, 405, { ok: false, status: 'method_not_allowed', serverRequestId }); const identity = route.actor(req); if (!identity) return route.send(res, 401, { ok: false, status: 'authentication_required', serverRequestId }); try { const body = await route.readBody(req); if (!route.validInput(body)) return route.send(res, 400, { ok: false, status: 'request_invalid', serverRequestId }); const result = await route.service().preflight({ ...body, ownerAccountId: identity.ownerAccountId, serverRequestId, responseLanguage: req.headers?.['accept-language']?.startsWith('de') ? 'de' : 'en' }, identity.user); return route.send(res, result.ok ? 200 : 409, { ...result, serverRequestId, clientRequestId: body.clientRequestId }); } catch { return route.send(res, 503, { ok: false, status: 'preflight_unavailable', serverRequestId }); } };
