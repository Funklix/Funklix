'use strict';
const crypto=require('crypto');
const {getSessionUser}=require('./_auth-session');
const {createStorage}=require('./social-connector/storage');
const config=require('./social-connector/linkedin-config');
const service=require('./social-connector/linkedin-service');
const settings=require('./social-connector/settings-projection');
function identityDiagnostic(env=process.env){const build=String(env.VERCEL_GIT_COMMIT_SHA||env.BUILD_ID||'bw32.2.9').slice(0,64).replace(/[^A-Za-z0-9._-]/g,'_');const secret=env.SESSION_SECRET||env.SOCIAL_TOKEN_ENCRYPTION_KEY,material=env.POSTGRES_URL;if(!secret||!material)return {build_identity:build};return {build_identity:build,database_identity_digest:crypto.createHmac('sha256',secret).update(material).digest('hex').slice(0,16)};}
module.exports=async function(req,res){const serverRequestId=service.requestId(req),user=getSessionUser(req),owner=service.accountId(user),diagnostic={phase:'request_authenticated',operation_category:'storage_unknown',...identityDiagnostic()};res.setHeader('content-type','application/json; charset=utf-8');res.setHeader('cache-control','no-store');if(req.method!=='GET'){res.statusCode=405;return res.end(JSON.stringify({error:{code:'method_not_allowed',serverRequestId}}));}if(!owner){res.statusCode=401;return res.end(JSON.stringify({error:{code:'authentication_required',serverRequestId}}));}const c=config.load();if(!c.ready)return res.end(JSON.stringify({linkedin:settings.project({configuration:c}),others:['instagram','facebook','x']}));try{const rows=await createStorage({diagnostic}).listConnectionProjection(owner);const row=rows.find(x=>x.platform==='linkedin'&&x.status!=='disconnected')||rows.find(x=>x.platform==='linkedin');diagnostic.phase='response_construction';diagnostic.operation_category='response_construction';res.end(JSON.stringify({linkedin:settings.project({configuration:c,row}),others:['instagram','facebook','x']}));}catch{res.statusCode=503;res.end(JSON.stringify({linkedin:settings.unavailable(serverRequestId,diagnostic),others:['instagram','facebook','x'],error:{code:'linkedin_storage_failed',serverRequestId}}));}};
