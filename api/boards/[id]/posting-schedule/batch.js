'use strict';
const crypto=require('crypto');const {getSessionUser}=require('../../../_auth-session');const {ensureBoardsTable}=require('../../../_boards-storage');const response=require('../../../_authoritative-response');const {validateBatchCommand}=require('../../../posting-schedule/batch-contract');const {createPostingScheduleBatchService}=require('../../../posting-schedule/batch-service');
function requestId(req){return response.requestId(req.headers?.['x-request-id'])||`req_${crypto.randomBytes(12).toString('base64url')}`;}function envelope(id,ok,status,extra={}){return{contract_version:response.CONTRACT_VERSION,ok,status,classification:status,server_request_id:id,...extra};}
module.exports=async function handler(req,res){const id=requestId(req);if(req.method!=='PUT')return response.write(res,405,envelope(id,false,'request_invalid',{failure_category:'method_not_allowed',retryable:false}));const actor=getSessionUser(req);if(!actor?.email&&!actor?.id)return response.write(res,401,envelope(id,false,'authentication_required',{failure_category:'authentication_required',retryable:false}));const validated=validateBatchCommand(req.body);if(!validated.ok||validated.value?.boardId!==req.query?.id)return response.write(res,400,envelope(id,false,'batch_rejected',{failure_category:validated.code||'invalid_board_id',retryable:false}));try{await ensureBoardsTable();const result=await createPostingScheduleBatchService().mutate(validated.value,actor);if(!result.ok)return response.write(res,result.httpStatus,envelope(id,false,'batch_rejected',{failure_category:result.code,retryable:result.httpStatus>=500}));return response.write(res,200,envelope(id,true,result.code,{board_revision:result.boardRevision,schedules:result.updated.map(x=>({node_id:x.nodeId,planning_schedule:x.planningSchedule,schedule_revision:x.scheduleRevision})),summary:{requested:validated.value.commands.length,updated:result.updated.length}}));}catch(_){console.error('[POSTING_SCHEDULE_BATCH_FAILURE]',{server_request_id:id,classification:'storage_unavailable'});return response.write(res,503,envelope(id,false,'batch_failed',{failure_category:'storage_unavailable',retryable:true}));}};
