(function(root){'use strict';let selected='';function initialize({document,fetchImpl,location,history,translate=t=>t}){const by=id=>document.getElementById(id),connect=by('facebook-connect-button'),disconnect=by('facebook-disconnect-button'),status=by('facebook-connection-status'),detail=by('facebook-connection-detail'),select=by('facebook-page-select'),label=by('facebook-page-label'),result=by('facebook-connection-result');let projection=null;
 function render(p){projection=p;const connected=p?.state==='connected';status.textContent=translate(connected?'Connected Page':p?.state==='needs_attention'?'Permission expired':p?.state==='setup_required'?'Setup required':'Not connected');connect.hidden=connected;connect.disabled=!(p?.connect_allowed||p?.reconnect_allowed);connect.textContent=translate(p?.reconnect_allowed?'Reconnect':'Connect Facebook');disconnect.hidden=!connected;disconnect.dataset.connectionId=p?.connection_id||'';select.replaceChildren();const pages=Array.isArray(p?.destinations)?p.destinations:[];select.hidden=label.hidden=!connected||!pages.length;if(connected&&!pages.length)detail.textContent=translate('No eligible Pages found');else if(connected){detail.textContent=translate('Facebook Page');const prompt=document.createElement('option');prompt.value='';prompt.textContent=translate('Select Page');select.append(prompt);pages.forEach(page=>{const option=document.createElement('option');option.value=page.id;option.textContent=`${page.label} · ${page.provider_identity}`;select.append(option)});if(pages.length===1){select.value=pages[0].id;selected=pages[0].id;}else selected='';}else detail.textContent=translate(p?.state==='needs_attention'?'Missing publishing permission':'Connect Facebook');}
 async function refresh(){try{const response=await fetchImpl('/api/social-connections',{headers:{accept:'application/json'}}),data=await response.json();render(data?.social_connections?.facebook||{state:'setup_required'});}catch{result.textContent=translate('Facebook is temporarily unavailable.');}}
 select.onchange=()=>{selected=select.value;result.textContent=selected?translate('Connected Page'):translate('Select Page');};connect.onclick=async()=>{connect.disabled=true;try{const response=await fetchImpl('/api/social-connections-facebook-start',{method:'POST',headers:{'content-type':'application/json','x-client-request-id':`fb_${crypto.randomUUID().replaceAll('-','')}`},body:'{"returnPath":"/"}'}),data=await response.json();if(!response.ok||!data.ok)throw new Error();location.assign(data.value.authorizationUrl);}catch{result.textContent=translate('Facebook could not complete the connection.');connect.disabled=false;}};disconnect.onclick=async()=>{disconnect.disabled=true;try{const response=await fetchImpl('/api/social-connections-facebook-disconnect',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({connectionId:disconnect.dataset.connectionId})});if(!response.ok)throw new Error();selected='';await refresh();result.textContent=translate('Facebook disconnected.');}catch{result.textContent=translate('Facebook could not be disconnected.');disconnect.disabled=false;}};
 try{const q=new URLSearchParams(location.search),notice=q.get('facebook');if(notice){result.textContent=translate(notice==='facebook_connected'?'Facebook connected.':notice==='facebook_cancelled'?'Connection cancelled.':'Facebook connection failed.');q.delete('facebook');q.delete('attempt_id');history.replaceState(history.state,'',`/${q.toString()?`?${q}`:''}${location.hash||''}`);}}catch{}refresh();return{refresh,getDestinationId:()=>selected};}
 root.FacebookSettings={initialize,getDestinationId:()=>selected};if(typeof module==='object'&&module.exports)module.exports=root.FacebookSettings;})(typeof globalThis!=='undefined'?globalThis:this);
